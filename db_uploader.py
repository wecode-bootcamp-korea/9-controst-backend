import os
import django
import csv
import sys

#os.environ.setdefault("DJANGO_SETTINGS_MODULE", "trost.settings")
#django.setup()
#from partner.models import Counselor, Theme, Counselor_Theme, Counsel_Kind, Counselor_Counsel_kind, Duration, Product

#CSV_PATH_COUNSELORS = './trost_crwaling_1(csv).csv'

#with open(CSV_PATH_COUNSELORS) as in_file:
#    data_reader = csv.reader(in_file)
#    next(data_reader, None)
#    for row in data_reader:
